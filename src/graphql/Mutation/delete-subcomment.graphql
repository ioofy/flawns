mutation subCommentDelete($subCommentId: ID!) {
  subCommentDelete(subCommentId: $subCommentId) {
    __typename
    userErrors {
      message
    }
    subComment {
      id
      text
    }
  }
}
