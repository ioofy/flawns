mutation signin($credentials: CredentialsInput!) {
  signin(credentials: $credentials) {
    userErrors {
      message
    }
    user {
      __typename
      name
      username
      avatarUrl
      isCreator
      profession {
        role
      }
    }
  }
}
