mutation updateUser($password: String!, $username: String!) {
  updateUser(password: $password, username: $username) {
    userErrors {
      message
    }
    user {
      __typename
      name
      username
      avatarUrl
      isCreator
      profession {
        role
      }
    }
  }
}
