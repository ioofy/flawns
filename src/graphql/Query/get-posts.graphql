query getPosts($postId: ID!) {
  getPost(postId: $postId) {
    content
    createdAt
    user {
      id
      name
      username
    }
    comments {
      id
    }
    likes {
      id
    }
  }
}
