query comments($offset: Int!, $limit: Int!, $postId: ID!) {
  comments(limit: $limit, offset: $offset, postId: $postId) {
    id
    __typename
    text
    createdAt
    user {
      avatarUrl
      name
      username
    }
  }
}
